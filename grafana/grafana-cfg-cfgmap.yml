apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: monitoring
data:
  grafana.ini: |
    [server]
    protocol = http
    domain = k3s.bulkington.ave
    root_url = https://k3s.bulkington.ave/grafana/
    serve_from_sub_path=true
